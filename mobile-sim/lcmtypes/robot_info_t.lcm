struct robot_info_t
{
    int64_t utime;

	// Waypoint region the robot is at (or "none")
	string current_waypoint;
    
    // Global pose of the robot
    double xyzrpy[6];
    
    // True if the robot is holding something
    int32_t held_object;
}
